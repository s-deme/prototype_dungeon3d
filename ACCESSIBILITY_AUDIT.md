# 可読性・人間工学監査

対象は、通常画面、マップ、タイトル、戦闘、祠、ポーズ、ガイド、プロフィール、設定、無効状態、狭い画面です。色だけを変えるのではなく、文字量、太さ、状態の伝え方、操作の見つけやすさを確認しました。

| 観点 | 見つかった問題 | 対応 |
| --- | --- | --- |
| 本文・補助文字 | 補助文字が細く暗く、パネル背景に埋もれる | 本文を15px太字、補助文字を13px太字へ引き上げ、明るい前景色へ統一 |
| UIサイズ | 80%まで縮小でき、読めない状態を選べた | 下限を90%、上限を140%へ変更。文字も設定値に連動して拡大 |
| 高コントラスト | 従来はマップの一部だけが変化した | 背景、パネル、罫線、文字、ボタン、ゲージ、マップ記号まで1テーマとして切替 |
| 無効ボタン | 保存なしの「続きから」が背景と同化し得る | 専用の背景色・明るい文字色を設定し、操作不可でも読める状態に変更 |
| マップ | 導きの点と回復薬の丸が似ており、色にも依存していた | 方向矢印へ変更し、説明も「矢印」と明記。目的物は形状とテキストで区別 |
| 進行ゲージ | 空のHP/集中力が暗すぎて残量を読めない | 非アクティブ部分を明るい青灰色に変更。数値も常時併記 |
| 封印出口・壁 | 暗い灰色が背景に沈む | 専用の高輝度色と壁アクセントを導入 |
| オーバーレイ | 説明文が通常ウェイトで小さい | 本文を太字・高コントラスト化し、戦闘・祠・ポーズ・チュートリアル・確認画面に反映 |
| 色覚多様性 | 色だけでの識別に寄りやすい | ◆/●/✦/✧/⌄/矢印と明示ラベル、HP/集中力の数値を併用 |
| タップ操作 | 記号だけの操作と誤操作の余地 | すべてのボタンに日本語の行動名とキー操作を併記。探索破棄は確認を必須化 |
| モーダル操作 | 前面の案内・設定から背面の操作が起こり得る | モーダル中は背面のIMGUI操作を無効化し、Escで閉じる経路を一貫させた |
| 入力の選択 | 固定キーと小さなタップ領域に依存 | キーボード再割り当て、ゲームパッド既定操作、最小44pxのタップ操作を追加 |
| 画面環境 | 解像度やウィンドウ状態を選べない | UI内から全画面／ウィンドウ、解像度、垂直同期を変更して保持できるようにした |
| スクリーンリーダー | IMGUIの画面内容が支援技術へ意味として渡らない | Unity Accessibility API の階層を、画面・モーダル・戦闘・探索の状態ごとに生成。目的、残り体力、選択肢、各アクションの名称とヒントを読み上げられるようにした。対応しない実行環境では安全に無効化する |

## 継続して守る基準

- 新しい文字色は、`DungeonPrototype.ApplyTheme()` のテーマ色だけを使う。個別の暗い灰色を追加しない。
- 情報を色だけで表さない。形、数値、短いテキストのうち少なくとも一つを併記する。
- 操作不能な要素も、読める文字色と理由を保つ。
- 新しい画面は通常テーマと高コントラストテーマの双方で確認する。
- モーダルを追加するときは、`BlocksBaseInput()` の対象に含め、背面の操作を必ず停止する。
- 新しい操作はキーボード、ゲームパッド、タップの少なくとも一つの導線と、画面上の説明を用意する。
- 操作を追加・削除したときは、`BuildAccessibilityNodes()` に等価な名称・ヒント・実行アクションを追加する。

## 採用色のコントラスト確認

パネル背景に対する代表色のコントラスト比を、相対輝度から確認しました。通常テーマでも本文17.12:1、補助文字12.36:1、シアン13.87:1、アンバー11.75:1、ピンク6.69:1、無効ボタン文字6.01:1です。高コントラストテーマでは本文19.39:1、補助文字15.44:1、強調色は7.79:1以上です。小さな文字は本文・補助文字だけを使用し、ピンクなどの強調色は見出し・状態表示に限定します。
